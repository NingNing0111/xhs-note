# 执行

1. 编辑`.env` 文件，填写配置信息。
2. 双击exe可执行文件。

## 配置说明

- `USER_DATA_DIR`: 浏览器数据目录，随便建一个目录设置上即可，浏览器数据均会存储在该目录下；
- `BROWSER_PATH`: 浏览器可执行路径。不填，则默认从注册表中读取默认浏览器。支持 Chrome、Edge。

**可执行路径获取**
- Chrome: 地址栏输入`chrome://version`
- Edge: 地址栏输入`edge://version`


## 获取数据 

- 参考: xhs_notes.xlsx